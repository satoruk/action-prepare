name: Sample

on:
  push:
    paths:
      - ".github/**"
      - "dist/**"
      - "examples/**"

jobs:
  sample:
    name: Sample step
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - macos-latest
          - ubuntu-latest
          - windows-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v2.3.1
      - uses: ./
        with:
          config-file: examples/demo.yml
      - if: ${{ matrix.os == 'windows-latest' }}
        name: sample
        run: |
          echo $env:FOO_TOKEN
          ls examples
      - if: ${{ matrix.os != 'windows-latest' }}
        name: sample
        run: |
          echo $FOO_TOKEN
          ls -al examples
